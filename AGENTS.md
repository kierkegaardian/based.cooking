# Repo Agent Notes

## Precedence
- Follow `/home/user/AGENTS.md` for workspace-wide rules and credential handling.

## Repo Summary
- Path: `/home/user/projects/based.cooking`.
- Stack: Hugo site using the lugo theme; content in `content/`.

## Notes (from README.md)
- New recipes should follow `example.md` and live in `content/`.
- Images live under `/pix` as small `.webp` files; filenames should match the recipe slug.
- Hugo dev server defaults to `http://localhost:1313`.

## Quality
- **Typesafety (Request):** Enforce robust, stack-appropriate typesafety in all changes (if adding scripts/tools, prefer TypeScript with strict types).
